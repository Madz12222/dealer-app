const express = require('express'); const CryptoJS = require('crypto-js'); const app = express(); app.use(express.json({ limit: '100mb' })); app.use((req, res, next) => { res.header('Access-Control-Allow-Origin', '*'); res.header('Access-Control-Allow-Headers', '*'); next(); }); app.post('/radar', async (req, res) => { let { startHash, depth = 100000 } = req.body, gameCache = [], currHash = startHash; console.log('Syncing 100K...'); for (let i = 0; i < depth; i++) { currHash = CryptoJS.SHA256(currHash).toString(); const hmac = CryptoJS.HmacSHA256(CryptoJS.enc.Hex.parse(currHash), '0000000000000000004d2049e604783873041b51909b5ca5b038a7f20169a38b').toString(); const ratio = 4503599627370496 / (parseInt(hmac.substring(0, 13), 16) + 1); const bust = Math.max(1, Math.floor((ratio * 99) / 100) / 100); gameCache.push({ b: bust, c: bust >= 2.00 ? 'G' : 'R' }); if (i % 20000 === 0) await new Promise(r => setImmediate(r)); } res.json({ success: true, cache: gameCache }); }); app.listen(3000, () => console.log('GHOST-RADAR v4 (STABLE) ONLINE'));
